const express = require('express');
const router = express.Router();

// Placeholder for booking routes
router.get('/', (req, res) => {
  res.json({
    success: true,
    message: 'Booking routes not yet implemented',
    data: []
  });
});

module.exports = router;